      <!doctype html>

<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Modelo de Site — Template Único</title>
  <meta name="description" content="Modelo de site responsivo, acessível e pronto para personalizar.">
  <!-- Fonte do Google (opcional) -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#0f1724; /* fundo escuro */
      --card:#0b1220;
      --muted:#94a3b8;
      --accent:#06b6d4; /* teal */
      --glass: rgba(255,255,255,0.04);
      --radius:12px;
      --max-width:1100px;
      --gap:20px;
      color-scheme: dark;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      font-family:Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:linear-gradient(180deg,var(--bg),#071126 120%);
      color:#e6eef6;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.5;
      padding:40px 16px;
      display:flex;justify-content:center;
    }/* container central */
.container{width:100%;max-width:var(--max-width)}

/* navegação */
.nav{
  display:flex;align-items:center;justify-content:space-between;margin-bottom:32px;
}
.brand{display:flex;align-items:center;gap:12px}
.logo{
  width:52px;height:52px;border-radius:12px;background:linear-gradient(135deg,var(--accent),#7c3aed);display:flex;align-items:center;justify-content:center;font-weight:700;font-size:18px;color:#021026;box-shadow:0 6px 18px rgba(6,182,212,0.12)
}
.nav-links{display:flex;gap:18px;align-items:center}
.nav-links a{color:var(--muted);text-decoration:none}
.cta{background:linear-gradient(90deg,var(--accent),#7c3aed);padding:10px 14px;border-radius:10px;color:#021026;font-weight:600;text-decoration:none}

/* hero */
.hero{display:grid;grid-template-columns:1fr 420px;gap:var(--gap);align-items:center;margin-bottom:36px}
.hero-card{background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);padding:28px;border-radius:var(--radius);box-shadow:0 6px 30px rgba(2,6,23,0.6)}
.hero h1{margin:0 0 8px;font-size:34px}
.hero p{margin:0 0 18px;color:var(--muted)}
.feature-list{display:flex;flex-wrap:wrap;gap:10px}
.feature{background:var(--glass);padding:10px 12px;border-radius:10px;font-size:14px;color:var(--muted)}

/* card lateral */
.side-card{background:linear-gradient(180deg,var(--card), rgba(255,255,255,0.02));padding:18px;border-radius:14px}
.side-card h3{margin:0 0 10px}

/* seção de serviços */
.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-bottom:30px}
.service{background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);padding:18px;border-radius:12px}
.service h4{margin:0 0 8px}
.service p{margin:0;color:var(--muted)}

/* galeria */
.gallery{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:30px}
.gallery img{width:100%;height:140px;object-fit:cover;border-radius:10px;display:block}

/* formulário */
.contact{display:grid;grid-template-columns:1fr 320px;gap:18px;padding:18px;background:linear-gradient(180deg, rgba(255,255,255,0.02), transparent);border-radius:12px}
.input, textarea{width:100%;padding:12px;border-radius:10px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
.btn{display:inline-block;padding:10px 16px;border-radius:10px;background:var(--accent);color:#021026;font-weight:700;text-decoration:none}

/* rodapé */
footer{margin-top:36px;color:var(--muted);font-size:14px;padding:18px 0;text-align:center}

/* responsivo */
@media (max-width:980px){
  .hero{grid-template-columns:1fr;}
  .grid{grid-template-columns:repeat(2,1fr)}
  .gallery{grid-template-columns:repeat(2,1fr)}
  .contact{grid-template-columns:1fr}
}
@media (max-width:560px){
  body{padding:24px 12px}
  .grid{grid-template-columns:1fr}
  .gallery{grid-template-columns:1fr}
  .nav-links{display:none}
}

/* pequena animação */
.fade-up{transform:translateY(8px);opacity:0;animation:fadeUp .7s ease forwards}
@keyframes fadeUp{to{transform:none;opacity:1}}

  </style>
</head>
<body>
  <div class="container">
    <!-- Navegação -->
    <nav class="nav" role="navigation" aria-label="Menu principal">
      <div class="brand">
        <div class="logo" aria-hidden="true">GS</div>
        <div>
          <div style="font-weight:700">Seu Projeto</div>
          <div style="font-size:13px;color:var(--muted)">Design moderno • Rápido • Responsivo</div>
        </div>
      </div>
      <div class="nav-links" role="list">
        <a href="#servicos">Serviços</a>
        <a href="#galeria">Galeria</a>
        <a href="#contato">Contato</a>
        <a class="cta" href="#contato">Contrate</a>
      </div>
    </nav><!-- Hero -->
<section class="hero">
  <div class="hero-card fade-up">
    <h1>Modelo de site profissional</h1>
    <p>Um template limpo, responsivo e fácil de personalizar. Ótimo para portfólios, pequenas empresas e projetos acadêmicos.</p>
    <div class="feature-list">
      <div class="feature">Responsivo</div>
      <div class="feature">Acessibilidade</div>
      <div class="feature">Performance otimizada</div>
      <div class="feature">Fácil customização</div>
    </div>
    <p style="margin-top:18px"><a class="btn" href="#contato">Solicitar orçamento</a></p>
  </div>

  <aside class="side-card fade-up" aria-labelledby="aside-title">
    <h3 id="aside-title">Destaques</h3>
    <ul style="margin:0;padding-left:18px;color:var(--muted)">
      <li>Layout em uma única página (one-page)</li>
      <li>Seções modulares</li>
      <li>Formulário de contato funcional (requer backend)</li>
    </ul>
  </aside>
</section>

<!-- Serviços -->
<section id="servicos">
  <h2 style="margin-top:0">Nossos serviços</h2>
  <div class="grid">
    <article class="service fade-up">
      <h4>Design e Branding</h4>
      <p>Criamos identidade visual coerente e moderna para sua marca.</p>
    </article>
    <article class="service fade-up">
      <h4>Desenvolvimento Web</h4>
      <p>Sites rápidos, acessíveis e adaptados para mobile.</p>
    </article>
    <article class="service fade-up">
      <h4>Manutenção e SEO</h4>
      <p>Otimização para buscadores e suporte contínuo.</p>
    </article>
  </div>
</section>

<!-- Galeria -->
<section id="galeria">
  <h2>Galeria</h2>
  <div class="gallery" aria-label="Exemplos de trabalhos">
    <img src="https://images.unsplash.com/photo-1506765515384-028b60a970df?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Projeto 1">
    <img src="https://images.unsplash.com/photo-1498050108023-c5249f4df085?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Projeto 2">
    <img src="https://images.unsplash.com/photo-1492724441997-5dc865305da7?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Projeto 3">
    <img src="https://images.unsplash.com/photo-1526378720452-4bc2b4b3a2a0?q=80&w=800&auto=format&fit=crop&ixlib=rb-4.0.3&s=placeholder" alt="Projeto 4">
  </div>
</section>

<!-- Contato -->
<section id="contato" style="margin-top:26px">
  <h2>Contato</h2>
  <div class="contact" role="region" aria-label="Formulário de contato">
    <form onsubmit="handleSubmit(event)" aria-label="Formulário principal">
      <label for="nome" style="display:block;margin-bottom:8px">Nome</label>
      <input id="nome" class="input" required placeholder="Seu nome">

      <label for="email" style="display:block;margin-top:12px;margin-bottom:8px">Email</label>
      <input id="email" type="email" class="input" required placeholder="seu@exemplo.com">

      <label for="mensagem" style="display:block;margin-top:12px;margin-bottom:8px">Mensagem</label>
      <textarea id="mensagem" rows="5" class="input" placeholder="Escreva sua mensagem..."></textarea>

      <div style="margin-top:12px">
        <button class="btn" type="submit">Enviar mensagem</button>
      </div>
    </form>

    <div style="padding:10px">
      <h3>Informações</h3>
      <p style="color:var(--muted)">Rua Exemplo, 123 • São Paulo • Brasil</p>
      <p style="color:var(--muted)">Telefone: (11) 9 0000-0000</p>
      <p style="color:var(--muted)">Email: contato@exemplo.com</p>
    </div>
  </div>
</section>

<footer>
  © <span id="year"></span> Seu Projeto • Todos os direitos reservados
</footer>

  </div>  <script>
    // ano no rodapé
    document.getElementById('year').textContent = new Date().getFullYear();

    // submissão do formulário (exemplo: mostra mensagem e limpa)
    function handleSubmit(e){
      e.preventDefault();
      const nome = document.getElementById('nome').value || 'Amigo';
      alert('Obrigado, ' + nome + '! Sua mensagem foi recebida.');
      e.target.reset();
    }

    // animação simples ao aparecer
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelectorAll('.fade-up').forEach((el,i)=>{
        el.style.animationDelay = (i*80)+'ms';
      });
    });
  </script></body>
</html>
